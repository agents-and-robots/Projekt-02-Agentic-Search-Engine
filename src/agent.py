def run(query: str, documents: list[str]) -> dict:
    """
    Baseline-Agent. Führt noch keine echte Suche aus.
    """
    return {
        "answer": "",
        "steps": [],
        "scores": []
    }
